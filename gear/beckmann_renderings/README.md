# Beckmann Scattering variations
Run 
```
drtvam config_0.00.json -Doutput="0.00"
```

for each of the config files. Then call `python plot_all2.py` to plot the scattered signal for different scattering paramters. 
